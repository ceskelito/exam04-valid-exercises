The last modified file is **vbc.c**

The input validation is **not working**. Please test only with valid inputs

The program must be able to parse even complex expressions with multiple level of parenthesis, but is **not sure**

I haven't cheked for memory leaks, so be carefull while tunning the program.

To compile it:
```bash
gcc vbc.c -o vbc
```

